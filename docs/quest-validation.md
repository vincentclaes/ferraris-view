# Quest 3 acceptance

Latest local package: **13 September 2026**, `builds/Winksele1775.apk`, **80,479,944 bytes** (76.75 MiB). SHA-256: `c13da07c2016566bbb86a06a2b22e92378aed6f2b3a0d1a4a6489a3b4843b28f`. Built from `544097f` on `codex/development-diagnostics` ([draft PR #26](https://github.com/vincentclaes/ferraris-view/pull/26)); this adds optional development diagnostics to merged mainline. Unity 6000.6.0f1 built Android ARM64 with OpenXR, Vulkan, minimum API 29 and target API 36. APK Signature Scheme v2 verifies. The manifest contains VR launch/headtracking declarations and Meta Quest supported-device metadata.

`bash scripts/unity.sh quest` now rebuilds generated Android staging/compiled output and runs `scripts/verify-quest.py`. The first package contained 100 numbered stale copies (156,619,994 bytes). Cleaning only Gradle staging exposed duplicate `mscorlib` assemblies in the Android compilation cache; recreating both generated Android directories fixed the failure. A later build failed the road-mask inclusion gate; rebuilding the Android player-data cache as well restored the packed-asset proof. The final package has no numbered copies or duplicate ZIP entries, contains the required ARM64 Unity/IL2CPP/OpenXR libraries, and embeds byte-identical world, terrain, five discovery JSON files, Liberation Sans font and its OFL license. The world contains the 53 reviewed building symbols. Receipt: `artifacts/quest-package-validation.json`; build log: `artifacts/unity-quest.log`.

The Android build report confirms that the imported road-mask texture and all 19 Marie voice clips are packed (`FERRARIS_ROAD_MASK_PACKED`, `FERRARIS_STORY_VOICE_PACKED 19`). Every build now rejects missing greeting, invitation, wait, rejoin, stop, question-answer or follow clips. These assets are imported/compressed by Unity; their original PNG/WAV bytes are not expected verbatim inside the APK. The reproducible source-mask hash and vector hash are in `artifacts/road-mask-validation.json`.

Hardware status: ADB found no attached device on 13 September. This package has **not** been installed or tested on the headset. The earlier simple prototype was installed and launched on 12 September; Vincent confirmed map → world → movement → B return, and a short sample reported 72–73 FPS. Those results do not validate the current graphics, polygon colliders or discovery interfaces. This package includes the merged desktop map-edge fix; Quest map pan remains bounded to its texture. It adds slimmer grain ears, bent leaves, wind, segment-based planting clearance and gradual ground-cover distance transitions. It includes controller-specific navigation instructions, the integrated navigation, voiced Marie guide and five animated tableaux, reviewed building contours/details, terrain roads and matched horizon. The same runtime source passed 33 Unity tests, 356 desktop journey checks, 116 map-edge checks on the preceding unchanged navigation source, five diagnostic checks and 21 controller replay checks. Browser release evidence is tracked separately in [web validation](web.md). Head tracking, controller usability, suspend/resume, comfort and sustained performance still need testing on this APK.

Connect an unlocked Quest 3 over USB with developer mode enabled and accept USB debugging inside the headset. Run the `quest-device.sh` commands in the README. Keep the headset's normal boundary active and begin seated or standing in a clear space.

Development diagnostics: Y on the left controller toggles coordinates, selected map point, terrain height and application frame cadence; F3 provides the same view on desktop. While enabled, the player emits one `FERRARIS_DIAGNOSTICS` line per second through Unity logging. Focus/pause changes restart the sample window. The readout measures delivered application frames per wall-clock second, not compositor refresh, reprojection, CPU work or GPU execution. Use it to correlate location with profiler/device captures, not to certify the 72 FPS target by itself. It is disabled by default and excluded from non-development players.

1. Launch from the installed app or the device script. The Ferraris map should appear ahead. Turn your head and confirm stable, correct stereo tracking in both eyes.
2. Use the left stick to pan after zooming with the right stick. Aim the right controller ray at a recognizable road junction and press its trigger.
3. Confirm the world opens at that junction, with terrain, dirt roads, buildings and orchard trees visible. Check the ground height feels plausible and the camera follows head movement.
4. Walk with the left stick and turn with the right stick. Turning should happen in 30-degree steps. Confirm movement follows gaze, buildings block passage, and you remain supported by the terrain.
5. Press B to return to the map. Confirm the selected marker corresponds to the entry point. Repeat from a field and a building symbol; building selections may move to a nearby free spawn within 25 metres.
6. Remove and replace the headset, then return from the system menu. Check tracking, input and the current mode recover. Note discomfort or unexpected camera movement.
7. Open Ontdek → Volg een dag in 1775. From map mode, choose Zoek Marie, approach her and select Ik loop mee. Check her voice and subtitles, ask a question, follow her through all five tableaux, fall behind and catch up. Verhaal verlaten must stop voice and movement immediately; return and choose Hervatten to continue with the same guide. Inspect residents and both source pages. Repeat with the story voice muted.
8. Profile a development build in Unity Profiler over the Android connection for at least five minutes, including the village and tree clusters. Record achieved refresh rate, frame-time spikes and sustained CPU/GPU timing. At 72 FPS the frame budget is approximately 13.9 ms. A desktop FPS reading or successful build is not a Quest performance result.

Record the date, Quest model/OS, Git commit, APK SHA-256, tracking/control results, measured performance and any errors from `bash scripts/quest-device.sh logs`. Distinguish the confirmed basic journey and short 72 Hz performance sample from longer stress, resume and comfort testing.

## Controller input replay — 13 September 2026

`bash scripts/unity.sh desktop` followed by `bash scripts/smoke-desktop.sh -xr-input-study` runs the controller bindings in a rendered desktop player. The replay supplies generic Input System HMD/controller events and bypasses only headset-presence detection. It does not simulate an OpenXR compositor or hardware tracking, and its receipt explicitly records `hardwareVerified: false`. Replay code is excluded from Android and WebGL players; its type and diagnostic strings are absent from the final APK metadata.

On runtime `ffd64a0`, all 19 checks passed: head-pose binding, joystick map zoom/pan, ray-selected coordinates after both transforms, world entry, B return with preserved selection, three proportional movement speeds with terrain support, gaze-oriented movement, a 30-degree turn around the eye, controller opening of the location map without moving the visitor, and B closing the panel before returning to selection. Sampled desktop speeds at 25/50/100 percent input were about 0.73/2.08/5.87 m/s, including a release frame; these are not headset measurements.

Visual inspection found desktop mouse/keyboard instructions still showing in VR after the remote navigation merge. The navigation now rebuilds when XR activates and displays controller instructions and labels; the replay checks these texts fit. Inspected evidence: `/tmp/ferraris-xr-input/xr-map.png`, `xr-world.png`, `xr-input.json` and `player.log`. The normal desktop workflow passed all 356 checks in a separate run. An earlier run overlapped a Unity build and timed out on a guide leg; focus loss pauses Marie by design, so run rendered player acceptance without another Unity build taking focus. The timeout now logs route state, positions, visibility and any obstructing collider.

Build workspace check: 68 newly appearing, untracked numbered copies under Unity Assets were byte-identical to their originals. They were preserved outside Assets in `artifacts/source-sync-quarantine-20260913/` before the build; no tracked source was removed. The final APK contains no numbered copies. Metadata also excludes `MapEdgeStudy`, `GroundFadeStudy`, `FieldStudy` and `XRInputStudy`.

Diagnostics validation: the built desktop player verifies default-hidden state, actual F3 toggling, live map and eye coordinates, terrain units, text fitting and pointer pass-through. The controller replay now includes Y show/hide and verifies world-space text fitting (21 checks total). Images were inspected in `/tmp/ferraris-diagnostics-desktop/` and `/tmp/ferraris-diagnostics-xr/`; their logs also verify frame-count/elapsed-time arithmetic and the 28m local-to-TAW datum. The normal journey still passes all 356 checks, and 33 Unity tests pass. The signed APK contains `DevelopmentDiagnostics` but excludes all desktop study code. These checks do not establish hardware performance.

The non-development WebGL release also builds successfully. Decompressed data, WebAssembly and JavaScript contain neither the diagnostic type, UI title nor log marker (UTF-8/UTF-16 checks). Receipt: `artifacts/diagnostics-release-exclusion.json`. This build was not deployed; the existing web preview and production remain unchanged.
